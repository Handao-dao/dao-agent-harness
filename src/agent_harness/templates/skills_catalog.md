# Available Skills

The catalog below contains only Skill discovery metadata. When a task matches a Skill,
call `activate_skill` before following that Skill. Load referenced UTF-8 text files later
with `read_skill_resource`; do not guess their contents.
